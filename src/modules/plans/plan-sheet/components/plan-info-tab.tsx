import { SheetInput } from '@/components/sheet-input'

// TODO: добавить вывод данных в массиве, когда будет готово api
export const PlanInfoTab = () => (
    <div className="info-wrapper">
        <SheetInput label="Название" initialValue="Оказание услуг" />
    </div>
)
